<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.healthy.appointment.mapper.SysUserMapper">
    <select id="findEnabledByUsername" resultType="com.healthy.appointment.entity.SysUser">
        SELECT id, username, password_hash, name, phone, role, status, created_at, updated_at
        FROM sys_user
        WHERE username = #{username}
          AND status = 1
        LIMIT 1
    </select>

    <select id="existsByUsername" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM sys_user WHERE username = #{username})
    </select>

    <select id="existsByPhone" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM sys_user WHERE phone = #{phone})
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="user.id">
        INSERT INTO sys_user (username, password_hash, name, phone, role, status)
        VALUES (#{user.username}, #{user.passwordHash}, #{user.name}, #{user.phone}, #{user.role}, 1)
    </insert>
</mapper>
